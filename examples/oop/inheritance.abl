class Greeter():
    set speak to (this):
        pr("hello")

class LoudGreeter(Greeter):
    set speak to (this):
        Greeter.speak(this)
        pr("HELLO")

set g to Greeter()
set lg to LoudGreeter()

g.speak()
lg.speak()
