class Greeter():
    fun speak(this):
        pr("hello")

class LoudGreeter(Greeter):
    fun speak(this):
        Greeter.speak(this)
        pr("HELLO")

g = Greeter()
lg = LoudGreeter()

g.speak()
lg.speak()
