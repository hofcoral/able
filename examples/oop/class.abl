class Person():

    fun init(this, first_name, last_name, age):
        this.first_name = first_name
        this.last_name = last_name
        this.age = age


    fun greet(this):
        pr("Hello from ", this.first_name, "!")

person = Person("Donald", "Draper", 45)

person.greet()
