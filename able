#!/bin/bash
# Simple runner for the Able interpreter
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BIN="$SCRIPT_DIR/bin/able"

if [ ! -x "$BIN" ]; then
    echo "Building interpreter..."
    make -C "$SCRIPT_DIR" >/dev/null || { echo "Build failed"; exit 1; }
fi

exec "$BIN" "$@"
